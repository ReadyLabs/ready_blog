= link_to t('visitors.sessions.new'), new_user_session_path
= link_to t('visitors.user_registrations.new'), new_user_registration_path

- if user_signed_in?
  = link_to t('posts.new'), new_post_path
  = link_to 'My Posts', user_posts_path(current_user)
