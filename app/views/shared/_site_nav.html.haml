= link_to t('users.index'), users_path

- if user_signed_in?

  = link_to t('posts.new'), new_post_path
  = link_to t('posts.browse'), user_posts_path(current_user)

  = link_to 'Sign Out',
    destroy_user_session_path,
    id: 'sign_out_link',
    method: 'delete'

